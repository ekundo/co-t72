#!/usr/bin/env python3
"""Диск D: в CO -- второй квазидиск.

CO знает три диска: A:, B: и квазидиск C:. Каталоги он читает через БДОС, так
что четвёртый диск ему по силам.

Меню выбора диска по клавише 7 у CO собрано из блока строк, за которым сразу
идёт код -- четвёртую строку дописать некуда, а пункты B: и D: включаются
независимо, так что порядок строк меняется. Поэтому меню целиком переехало в
хвост: список собирается в буфере под текущие настройки, буквы лежат в такой же
таблице рядом.

Запись на D: -- разметка диска, а не порт
-----------------------------------------
Порт переключать не надо вовсе. Второй квазидиск сидит на порту 11h, но в CO
`OUT 11h` не нужен: свои прямые обращения к квазидиску (`OUT 10h` по 03B9 03DA
03FA 040C 0D9A 0DEA 1CED 1CF2 348F 34B4) CO делает только на ветке 'C', а на
D: он идёт через дисковый вызов БСВВ -- и там ОС сама выбирает порт по номеру
диска, ровно такой же самомодификацией операнда:

    L_E2C4: MOV C,M / ... / JNC L_KD        ; диск >= 2 -- это квазидиск
    L_KD:   LDA L_E98A / ADI 0Eh / STA L_E352+1   ; диск 2 -> 10h, диск 3 -> 11h

(Source/_E200h.asm в MDOS_T-72). Диск C: у CO -- номер 2, D: -- номер 3, так
что запись на D: через ОС попадает на нужный порт без единой правки в CO.

Ломали копирование две проверки буквы, которые решают не «куда писать», а «как
устроен диск»:

    1DE6  MOV A,M / CPI 'C' / MVI A,40h / JZ 1DF0   сколько записей в каталоге
    1EC2  LDA A849h / CPI 'C'                       ширина номера блока в записи

Квазидиск маленький, номера блоков в нём влезают в байт (16 штук на запись);
дискета на 390 блоков требует слова (8 штук на запись). Для D: CO брал
дискетный вариант и читал каталог второго квазидиска парами байт. Из пары
(блок, блок) получался адрес A1xx + старший байт, и CO рассыпал 5Ah по всей
памяти от A100 до D500 -- вместе со своим стеком у BC00 и перенесённым туда
хвостом. Карта исполненных адресов (EMU80_COV_*) показывала это как срыв в
буфер копирования: последний осмысленный адрес 2A09, дальше сплошной прогон
5A96..63C0 -- байты самого ARC.COM, исполняемые как код.

Обе проверки заменены на месте вызовом общей: 'C'-43h = 0, 'D'-43h = 1, младший
бит отбрасывается -- Z для обеих букв, 'A' и 'B' дают FEh. Трамплин не нужен:
CALL вместе с загрузкой буквы короче исходного CPI с загрузкой.

Проверено на стенде с двумя квазидисками (out/co-t72.edd как C:, образ с
9 файлами как D:):
  * панель открывает D:, читает каталог и показывает его верно;
  * копия A:ARC.COM на D: проходит: было 9 файлов, стало 10, ARC.COM на месте
    и после перечитывания панели;
  * C: не задет: копия на C: работает как раньше, панель гоняется
    D: -> C: -> D: -> C: без ошибок чтения.

Что показала проверка на новой T-72 (15.09.2026):

  * панель D: открывается, каталог второго квазидиска читается;
  * копирование идёт, три файла доходят целыми;
  * на четвёртом -- ошибка БСВВ на записи каталога: D=22 (БДОС «создать
    файл»), ST=02 (не сошлась контрольная сумма сектора), T=0 S=2.

Окно A000-DFFF тут ни при чём, хотя сначала показалось обратное. В обмене с
квазидиском БСВВ гасит первый КД (XRA A / OUT 10h) и шлёт конфиг на порт
выбранного диска -- 10h или 11h. Конфиг считается по дорожке, и в выпущенных
ромах это ANI 0Ch / ORI 010h: взведён только бит 4 (стековый режим), бит 5
(подмена окна A000-DFFF) не взведён. На гитхабе в исходниках там ORI 033h, но
собранные ромы -- с 010h, проверено по всем четырём.

Отсюда: во время ЛЮБОГО обмена с квазидиском, хоть C:, хоть D:, окно показывает
экран, а не ОЗУ. Никакой разницы между первым и вторым КД нет, и буферы CO
ради D: переносить не нужно. Обмен и так идёт через 4100 и 6100 -- ниже A000.

Что проверять дальше: сам образ второго квазидиска. Три файла записались, то
есть карта на месте и запись работает; ошибка пришла на секторе 2 нулевой
дорожки -- это вторая запись каталога, входы 4..7. Первая (входы 0..3) прошла.
И CO ещё до копирования говорил «файл уже есть» на заведомо пустом диске, то
есть каталог читался не пустым. Похоже, образ подготовлен не тем, чем надо:
контрольные суммы секторов считает сама ОС, и правильно разметить квазидиск
может только её команда «8 D: F» (P_E5D0, второй КД она поддерживает). Значит
сначала формат средствами ОС, потом копирование.

Разобранные тупики -- не проверять заново
-----------------------------------------
* Переключать порт квазидиска в CO не нужно и нечем помочь: на D: ни одна из
  десяти команд `OUT 10h` не исполняется. Ставить порт по букве в одном месте
  (3E9B) вредно -- он «залипает» и ломает чтение C:.
* Внутрь процедуры записи 03DE зайти вызовом нельзя: она прячет SP в 00F0,
  подключает страницу и ставит стек на данные командой SPHL. RET возвращаться
  некуда.
* Подмена CALL 03DE у вызывающих (2A17, 2E4E, 3AB9, 3F58) разваливает экран.
* Восемь мест с `CPI 43h` (CHECKS_OFF) уводить на ветку C: не надо: из них
  разметку задают только 1DE7 и 1EC5 (они и переписаны здесь по-другому),
  0B5F и 0B6E выбирают строку заголовка панели, 0262 и 027A ломают чтение
  каталога, а 3E9B увёл бы запись на прямой доступ к первому квазидиску.

Доступность D: пока зашита в самом коде ('Y' по адресу метки dflag). Настройку в
параметрах и её хранение ещё предстоит сделать: в CO.PRM для флага годится
только хвост файла за пределами читаемого старым CO, а буква панели там
упакована в два бита (1=A, 2=B, 3=C) -- под D: остаётся свободный бит 6.

Тем же измерением проверен и второй способ: те же сравнения можно переписать не
подпрограммой, а заменой JZ на JNC (три байта: 1DEB, 1ECC, 3E9D) -- «не меньше
C:» вместо «равно C:». Работает так же; здесь оставлен вариант с подпрограммой,
он не зависит от того, какая буква у диска следующая по алфавиту.
"""

import argparse
import os
import sys

ORG = 0x100
RUNTIME = 0xB740      # куда relocstub.py уводит хвост (было B700 -- см. его шапку)

# точки входа CO
SEL_PANEL = 0x0E4D     # HL += 1, если активна вторая панель
MENU = 0x0CF1          # выбор из горизонтального меню: HL -- строки, D -- сколько, E -- текущий
REDRAW = 0x0FE0        # перерисовать панели
REREAD = 0x28AE        # хвост «7-Диск»: записать букву из B и перечитать панель
PANEL_DRV = 0x3E91     # буквы дисков панелей
DISK_B = 0x3E97        # настройка «Диск B доступен» -- 'Y' или 'N'

MENU_ENTRY = 0x2870    # начало обработчика «7-Диск»
MENU_END = 0x28AE      # общий хвост, его оставляем на месте
MENU_REF = 0x0600      # запись «клавиша 7 -> обработчик» в таблице клавиш

# Места, где 'C' означает «это квазидиск». Для каждого: адрес, сколько байт
# занимает проверка вместе с тем, что к ней прицеплено, и как её пересобрать.
#   'ret'  -- CPI 'C' / RZ
#   'jz'   -- CPI 'C' / JZ target
#   'jz+a' -- CPI 'C' / MVI A,n / JZ target        (MVI выполняется всегда)
#   'jz+ah'-- CPI 'C' / MVI A,n / LXI H,nn / JZ target
CHECKS_OFF = [
    (0x0262, 'ret', 3),
    (0x027A, 'ret', 3),
    (0x0B5F, 'jz', 5),
    (0x0B6E, 'jz', 5),
    (0x1DE7, 'jz+a', 7),
    (0x1EC5, 'jz+ah', 10),
    (0x3E9B, 'jz', 5),
]
# Разобранный тупик, см. шапку. Разметку диска задают только 1DE7 и 1EC5, и они
# переписаны иначе -- см. GEOM; остальные пять уводить на ветку C: не надо.
# По 0B5F и 0B6E CO выбирает не разметку, а сами подпрограммы доступа: для C:
# квазидисковые (1713 и 178F), для всего прочего -- дисководные (1701 и 179B), а
# у тех каталог на 128 входов и лежит с дорожки 8. Пока диск 3 уходил по
# собственному пути CO, разницы было не видно; теперь, когда обмен идёт через
# БСВВ, она вылезает: каталог D: читается по-квазидисковому, а пишется
# по-дисководному -- 32 записи из 5000-5FFF на дорожку 8. Отсюда и
# рассогласование контрольных сумм. Уводим D: в ту же ветку, что и C:.
CHECKS = [
    (0x0B5F, 'jzcd', 5),
    (0x0B6E, 'jzcd', 5),
]

# Прямые обращения к квазидиску: XRA A / OUT 10h -- подключить страницу диска,
# MVI A,23h / OUT 10h -- вернуть память. Второй квазидиск сидит на порту 11h,
# косвенного OUT у 8080 нет, поэтому операнд правим на месте.
# Обвязка у всех обращений одинаковая: XRA A / OUT 10h -- подключить страницу
# квазидиска, MVI A,23h / OUT 10h -- вернуть основную память. Обе меняем на вызов
# подпрограммы, которая смотрит текущий диск и выбирает порт: у первого квазидиска
# он 10h, у второго 11h. Размер сходится байт в байт -- 3 и 4.
BANK_ON = [0x03B8, 0x03F9, 0x0D99, 0x348E]
BANK_OFF = [0x03D8, 0x040A, 0x0DE8, 0x34B2]
DRIVE = 0xB673         # букву текущего диска CO кладёт сюда (3E9B: STA B673h)

DIRDRV = 0xA849        # буква диска, каталог которого CO разбирает сейчас

# Разметка диска: обе проверки выбирают между «как у квазидиска» и «как у
# дискеты». Заменяются на месте, трамплин не нужен -- вызов проверки короче
# исходного CPI вместе с загрузкой буквы.
#   1DE6: MOV A,M / CPI 'C' / MVI A,40h / JZ 1DF0   -- сколько записей в каталоге
#   1EC2: LDA A849 / CPI 'C'                        -- ширина номера блока
GEOM = [
    (0x1DE6, 'cdm', b'\x7e\xfe\x43\x3e\x40\xca\xf0\x1d', b'\x3e\x40\xca\xf0\x1d'),
    (0x1EC2, 'cdl', b'\x3a\x49\xa8\xfe\x43', b''),
]

WRITE = 0x03DE         # запись сектора на квазидиск
WRITE_CALLERS = [0x2A17, 0x2E4E, 0x3AB9, 0x3F58]
WRITE_PORTS = [0x03FA, 0x040C]     # операнды OUT внутри записи


class Asm:
    def __init__(self, org):
        self.org, self.code, self.labels, self.fixups = org, bytearray(), {}, []

    def label(self, name):
        self.labels[name] = self.org + len(self.code)

    def here(self):
        return self.org + len(self.code)

    def db(self, *b):
        self.code += bytes(b)

    def word(self, op, value):
        self.code += bytes([op, value & 0xFF, value >> 8])

    def ref(self, op, name):
        self.code += bytes([op])
        self.fixups.append((len(self.code), name))
        self.code += b'\0\0'

    def bytes(self):
        out = bytearray(self.code)
        for at, name in self.fixups:
            if name not in self.labels:
                sys.exit('нет метки %s' % name)
            out[at] = self.labels[name] & 0xFF
            out[at + 1] = self.labels[name] >> 8
        return bytes(out)


def build_ports(at):
    """Порт квазидиска перед записью -- и сразу переход в саму запись.

    Внутрь процедуры записи не зайти ни вызовом, ни ничем: она прячет SP в 00F0,
    ставит стек на данные командой SPHL и гонит их чередой PUSH. Поэтому порт
    выставляем до входа, у вызывающих: подменяем их CALL на свой, правим два
    операнда OUT по букве текущего диска и прыгаем в запись -- её RET вернётся
    к вызывающему сам.
    """
    a = Asm(at)
    a.label('kdwrite')
    a.db(0xF5, 0xE5)                               # PUSH PSW / PUSH H
    a.db(0x3E, 0x10)                               # MVI A,10h -- порт первого квазидиска
    a.word(0x3A_00 >> 8 | 0x3A, DRIVE) if False else None
    a.word(0x21, DRIVE); a.db(0x7E)                # LXI H,drive / MOV A,M
    a.db(0xFE, 0x44)                               # CPI 'D'
    a.db(0x3E, 0x10)                               # MVI A,10h (флаги не трогает)
    a.ref(0xC2, 'wput')
    a.db(0x3E, 0x11)                               # MVI A,11h
    a.label('wput')
    for port in WRITE_PORTS:
        a.word(0x21, port); a.db(0x77)             # LXI H,операнд / MOV M,A
    a.db(0xE1, 0xF1)                               # POP H / POP PSW
    a.word(0xC3, WRITE)
    return a.bytes(), a.labels


def build(at, image, ports_labels, flag_addr=None):
    """Собрать хвост: трамплины проверок и меню выбора диска."""
    a = Asm(at)
    a.labels.update(ports_labels)
    trampolines = {}

    # Данные держим в начале хвоста: конец резидента вплотную подходит к стеку
    # CO на BC00, и всё, что окажется под ним, стек рано или поздно затрёт.
    a.ref(0xC3, 'start')
    a.label('dflag'); a.db(0x4E)         # выставляется при открытии меню
    a.label('letters'); a.code += bytes(6)
    a.label('strbuf'); a.code += bytes(24)
    a.label('start')

    # ---------------- «квазидисковая» разметка для D: ----------------
    # Две проверки буквы решают не «куда писать», а «как устроен диск»: сколько
    # записей в каталоге (40h против 80h) и какой ширины номера блоков в записи
    # (байт против слова). Квазидиск в T-72 маленький, номера блоков в него
    # влезают в байт; дискета на 390 блоков требует слова. Второй квазидиск --
    # такой же, как первый, поэтому обе проверки должны отвечать на D: так же,
    # как на C:.
    #
    # Проверка возвращает Z для обеих букв: 'C'-43h = 0, 'D'-43h = 1, и младший
    # бит отбрасывается; 'A' и 'B' дают FEh и FFh -- после отбрасывания FEh.
    a.label('cdm')                       # буква лежит по HL
    a.db(0x7E, 0xD6, 0x43, 0xE6, 0xFE, 0xC9)
    a.label('cdl')                       # буква лежит в A849h
    a.word(0x3A, DIRDRV); a.db(0xD6, 0x43, 0xE6, 0xFE, 0xC9)

    # ---------------- трамплины «C: или D:» ----------------
    for addr, kind, size in CHECKS:
        off = addr - ORG
        assert image[off:off + 2] == b'\xfe\x43', hex(addr)
        back = addr + size
        trampolines[addr] = a.here()
        # Для D: только переключаем порт и идём дальше обычным путём: увести его
        # в ветку C: нельзя -- проверено, панели начинают сыпать ошибками диска.
        if kind == 'ret':
            a.db(0xFE, 0x43); a.ref(0xCC, 'port10'); a.db(0xC8)   # 'C' -- порт 10h и назад
            a.db(0xFE, 0x44); a.ref(0xCC, 'port11')               # 'D' -- порт 11h
            a.word(0xC3, back)
        elif kind == 'jzcd':
            # Оба квазидиска -- в одну ветку. Порт тут выставлять не нужно: его
            # по букве текущего диска правят BANK_ON/BANK_OFF и подмена записи.
            target = image[off + 3] | image[off + 4] << 8
            a.db(0xFE, 0x43); a.word(0xCA, target)
            a.db(0xFE, 0x44); a.word(0xCA, target)
            a.word(0xC3, back)
        elif kind == 'jz':
            target = image[off + 3] | image[off + 4] << 8
            a.db(0xFE, 0x43); a.ref(0xCC, 'port10'); a.word(0xCA, target)
            a.db(0xFE, 0x44); a.ref(0xCC, 'port11')
            a.word(0xC3, back)
        elif kind == 'jz+a':
            val = image[off + 3]
            target = image[off + 5] | image[off + 6] << 8
            name = 'q%04x' % addr
            a.db(0xFE, 0x43); a.ref(0xCC, 'port10'); a.ref(0xCA, name)
            a.db(0xFE, 0x44); a.ref(0xCC, 'port11')
            a.word(0xC3, back)
            a.label(name)
            a.db(0x3E, val); a.word(0xC3, target)
        elif kind == 'jz+ah':
            val = image[off + 3]
            hl = image[off + 5] | image[off + 6] << 8
            target = image[off + 8] | image[off + 9] << 8
            name = 'q%04x' % addr
            a.db(0xFE, 0x43); a.ref(0xCC, 'port10'); a.ref(0xCA, name)
            a.db(0xFE, 0x44); a.ref(0xCC, 'port11')
            a.word(0xC3, back)
            a.label(name)
            a.db(0x3E, val); a.word(0x21, hl); a.word(0xC3, target)

    # ---------------- меню выбора диска ----------------
    # Собираем список пунктов под текущие настройки: A: и C: есть всегда, B: и
    # D: -- по своим признакам. Строки складываем в буфер, буквы -- в таблицу
    # рядом, чтобы по номеру пункта сразу получить букву.
    menu = a.here()
    # Есть ли второй квазидиск -- спрашиваем у самой ОС. БСВВ «выбрать диск»
    # (C01B) возвращает ноль, если диска нет: число дисков система подставляет
    # себе при старте, и ответ верен на любой сборке T-72, а не только на новой
    # с таблицей по FFC9. Спрашиваем при каждом открытии меню -- это дешевле
    # пусковой подпрограммы и всегда свежо.
    #
    # Выбор надо вернуть: у T-72 «выбрать диск» заодно кладёт номер в описатель
    # дисковой операции, и без возврата следующее же обращение БДОС ушло бы на
    # чужой диск. Текущий диск лежит в младшей тетраде ячейки 0004.
    a.db(0x0E, 0x03); a.word(0xCD, 0xC01B)        # MVI C,3 / CALL SELDSK
    a.db(0x7C, 0xB5)                              # MOV A,H / ORA L
    a.db(0x3E, 0x4E)                              # MVI A,'N'
    a.ref(0xCA, 'dset')
    a.db(0x3E, 0x59)                              # MVI A,'Y'
    a.label('dset')
    a.ref(0x32, 'dflag')
    a.word(0x3A, 0x0004); a.db(0xE6, 0x0F, 0x4F)  # LDA 0004 / ANI 0Fh / MOV C,A
    a.word(0xCD, 0xC01B)                          # CALL SELDSK -- вернуть выбор
    a.ref(0x21, 'strbuf')
    a.db(0x36, 0x00)                     # первая строка пустая -- это заголовок
    a.db(0x23)                           # HL -> первый пункт
    a.ref(0x11, 'letters')               # DE -> таблица букв
    a.db(0x0E, 0x02)                     # C -- сколько пунктов набрали

    # Пункты идут по алфавиту: A, B, C, D. Дисководов в машине может быть один,
    # второго квазидиска может не быть -- эти два пункта условные, а счётчик
    # пунктов начат с двух, потому что A: и C: есть всегда.
    a.db(0x3E, 0x41); a.ref(0xCD, 'add')          # "A"
    a.word(0x3A, DISK_B); a.db(0xFE, 0x59)        # диск B доступен?
    a.ref(0xC2, 'noB')
    a.db(0x3E, 0x42); a.ref(0xCD, 'add'); a.db(0x0C)
    a.label('noB')
    a.db(0x3E, 0x43); a.ref(0xCD, 'add')          # "C"
    if flag_addr is None:
        a.ref(0x3A, 'dflag')
    else:
        a.word(0x3A, flag_addr)
    a.db(0xFE, 0x59)                              # диск D доступен?
    a.ref(0xC2, 'noD')
    a.db(0x3E, 0x44); a.ref(0xCD, 'add'); a.db(0x0C)
    a.label('noD')

    # текущий пункт: ищем букву активной панели в таблице букв
    a.word(0x21, PANEL_DRV); a.word(0xCD, SEL_PANEL); a.db(0x7E)   # A -- буква панели
    a.ref(0x21, 'letters')
    a.db(0x1E, 0x01)                     # E -- номер пункта
    a.db(0x41)                           # MOV B,C -- сколько всего пунктов
    a.label('find')
    a.db(0xBE); a.ref(0xCA, 'found')     # CMP M / JZ
    a.db(0x23, 0x1C)                     # INX H / INR E
    a.db(0x05); a.ref(0xC2, 'find')      # DCR B / JNZ
    a.db(0x1E, 0x01)                     # не нашли -- встанем на первый пункт
    a.label('found')

    a.db(0x79); a.ref(0x32, 'nitems')    # MOV A,C / запомнить, сколько пунктов
    a.db(0x57)                           # MOV D,A
    a.db(0x14)                           # INR D -- и заголовок
    a.ref(0x21, 'strbuf')
    a.word(0xCD, MENU)
    a.db(0xFE, 0x1B); a.word(0xCA, REDRAW)        # АР2 -- ничего не меняем
    # Номер пункта должен быть от 1 до числа пунктов. Ноль или больше нужного
    # меню отдаёт, если выбор отменили краем: без проверки DCR A давал бы FFh,
    # а выборка по таблице букв читала бы байт ПЕРЕД ней. Такая буква уходила в
    # панель и сохранялась в CO.PRM -- после перезапуска панель открывалась на
    # несуществующем диске.
    a.db(0xB7); a.word(0xCA, REDRAW)              # ORA A / JZ -- ноль
    a.db(0x3D)                                    # номер пункта с нуля
    a.ref(0x21, 'nitems'); a.db(0xBE)             # CMP M
    a.word(0xD2, REDRAW)                          # JNC -- за краем
    a.ref(0x21, 'letters')
    a.db(0x5F, 0x16, 0x00, 0x19)                  # HL += A
    a.db(0x46)                                    # B -- выбранная буква
    a.word(0xC3, REREAD)
    a.label('nitems'); a.db(0x00)

    # add: положить строку «"X"» в буфер по HL и букву из A в таблицу по DE
    a.label('add')
    a.db(0xF5)                                    # PUSH PSW
    a.db(0x36, 0x22, 0x23)                        # '"'
    a.db(0x77, 0x23)                              # буква
    a.db(0x36, 0x22, 0x23)                        # '"'
    a.db(0x36, 0x00, 0x23)                        # конец строки
    a.db(0xF1)                                    # POP PSW
    a.db(0x12, 0x13)                              # STAX D / INX D
    a.db(0xC9)

    return a.bytes(), trampolines, menu, a.labels


def main():
    p = argparse.ArgumentParser()
    p.add_argument('infile')
    p.add_argument('outfile')
    p.add_argument('--flag', help='адрес байта «диск D доступен» (Y/N); по умолчанию свой')
    p.add_argument('--only', help='включить только эту проверку (адрес), для разбора по одной')
    args = p.parse_args()

    if args.only:
        global CHECKS
        CHECKS = [c for c in CHECKS_OFF if c[0] == int(args.only, 16)]
    d = bytearray(open(args.infile, 'rb').read())
    if len(d) % 128:
        sys.exit('образ не выровнен по записи: %d байт' % len(d))

    # Фильтр дисковых вызовов. CO перехватывает вектор БСВВ и по 0185 смотрит
    # на номер диска из описателя: двойка -- первый квазидиск -- уходит к БСВВ
    # сразу, остальное идёт по собственному пути CO, рассчитанному на
    # дисководы (свой разбор дорожек и секторов с 01C5). Второй квазидиск --
    # диск 3 -- проваливался туда же, и обмен с ним шёл мимо БСВВ.
    #
    #     0185  MOV A,M
    #     0186  CPI 02h
    #     0188  JZ <обработчик>     ->   JNC <обработчик>
    #
    # Один байт: CPI оставляет перенос, если номер меньше двух, поэтому JNC
    # пропускает к БСВВ оба квазидиска, а дисководы 0 и 1 идут как и раньше.
    if d[0x0188 - ORG] != 0xCA:
        sys.exit('по 0188 ожидался JZ, а лежит %02X' % d[0x0188 - ORG])
    d[0x0188 - ORG] = 0xD2

    at = RUNTIME + (len(d) - 0x4000)
    ports_code, ports_labels = build_ports(MENU_ENTRY + 3)   # первые три байта -- переход в меню
    code, tramp, menu, labels = build(at, d, ports_labels,
                                      int(args.flag, 16) if args.flag else None)

    only = os.environ.get('DSEL_ONLY')
    for addr, name, was, tail in GEOM:
        if only and '%04X' % addr != only:
            continue
        off = addr - ORG
        if bytes(d[off:off + len(was)]) != was:
            sys.exit('не узнаю разметочную проверку по %04X' % addr)
        new = bytes([0xCD, labels[name] & 0xFF, labels[name] >> 8]) + tail
        if len(new) > len(was):
            sys.exit('проверка по %04X не помещается: %d при %d'
                     % (addr, len(new), len(was)))
        d[off:off + len(was)] = new + b'\x00' * (len(was) - len(new))

    for addr, kind, size in CHECKS:
        off = addr - ORG
        d[off:off + size] = bytes([0xC3, tramp[addr] & 0xFF, tramp[addr] >> 8]) + b'\x00' * (size - 3)
    # Меню целиком уезжает в хвост, общий хвост на 28AE остаётся на месте, а в
    # освободившиеся байты ложится переключатель порта.
    if len(ports_code) > MENU_END - MENU_ENTRY - 3:
        sys.exit('переключатель порта не влезает: %d байт при %d свободных'
                 % (len(ports_code), MENU_END - MENU_ENTRY - 3))
    # Подмена вызовов записи пока выключена -- см. шапку.
    d[MENU_ENTRY - ORG:MENU_ENTRY - ORG + 3] = bytes([0xC3, menu & 0xFF, menu >> 8])
    d[MENU_ENTRY - ORG + 3:MENU_ENTRY - ORG + 3 + len(ports_code)] = ports_code
    d[MENU_ENTRY - ORG + 3 + len(ports_code):MENU_END - ORG] = \
        b'\x00' * (MENU_END - MENU_ENTRY - 3 - len(ports_code))

    d += code
    while len(d) % 128:
        d += b'\0'
    open(args.outfile, 'wb').write(bytes(d))
    print('диск D: код по %04X, меню %04X, трамплинов %d' % (at, menu, len(tramp)))


if __name__ == '__main__':
    main()
