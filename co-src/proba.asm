; ==========================================================================
; ПРОБА СОСТАВА ОБОРУДОВАНИЯ: работает по адресу загрузки
; ==========================================================================
;
; Лежит за тем, что едет под стек, и именно поэтому под стек не едет: стаб по
; 40DB переносит ровно кусок STKFILE..STKEND, а всё, что дописано после него,
; остаётся там, куда его положила загрузка. Под стеком тесно, а проба нужна
; один раз при старте -- к первому чтению каталога, которое затрёт это место,
; она уже отработает.
; --------------------------------------------------------------------------
ORIGIN  EQU $
PROBA   EQU $
#include "../tools/hdprobe.asm"
PROBAEND EQU $
